<%= form_for @monument_collection do |f| %>
	<% if @monument_collection.errors.any? %>
    <div>
      <ul class="alert alert-danger" role="alert">
      <% @monument_collection.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="form-group">
    <%= f.label :name %><br>
    <%= f.text_field :name, class: "form-control" %>
  </div>
  <div>
    <%= f.submit 'Save', class: 'btn btn-primary save-button' %>
  </div>
<% end %>
